<div class="p-4 bg-gray-200 sticky top-0">
	<nav class="space-y-4">
		<a href="{{ path('app_account') }}" class="block px-4 py-2 text-gray-800 hover:bg-blue-500 hover:text-white rounded">
			Mon compte
		</a>
        {% if is_granted('ROLE_COMPANY') %}
            <a href="{{ path('app_company_edit') }}" class="block px-4 py-2 text-gray-800 hover:bg-blue-500 hover:text-white rounded">
                Mon entreprise
            </a>
            <a href="{{ path('app_company_offer_list') }}" class="block px-4 py-2 text-gray-800 hover:bg-blue-500 hover:text-white rounded">
                Mes offres
            </a>
        {% endif %}
        {% if is_granted('ROLE_STUDENT') %}
            <a href="{{path('app_student_applications')}}" class="block px-4 py-2 text-gray-800 hover:bg-blue-500 hover:text-white rounded">
                Mes candidatures
            </a>
        {% endif %}
	</nav>
</div>

